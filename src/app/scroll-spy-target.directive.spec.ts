import { ScrollSpyTargetDirective } from './scroll-spy-target.directive';

describe('ScrollSpyTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollSpyTargetDirective();
    expect(directive).toBeTruthy();
  });
});
